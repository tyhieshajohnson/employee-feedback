# employee-feedback
A small web app where users can submit employee feedback and view it instantly

# What the App Does

User fills in a form (Name, Department, Feedback)

Data is saved to MySQL

Feedback is displayed below the form

Uses AJAX (JS) so the page doesn’t reload